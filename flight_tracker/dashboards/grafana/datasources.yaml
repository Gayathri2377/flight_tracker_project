apiVersion: 1

datasources:
  - name: FlightTracker
    type: frser-sqlite-datasource
    access: proxy
    uid: flight_duckdb
    jsonData:
      path: /data/warehouse/flights.duckdb
    isDefault: true
    editable: true
